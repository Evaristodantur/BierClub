<!DOCTYPE html>
<html lang="es">  
	<head>
		<title>BierClub - Iniciar Sesión</title>
		<%- include('../partials/head.ejs') %>
		<script src="/js/login.js" type="text/javascript"></script>
</head>

<body>

	<!-- NAV -->
	<header>
		<%- include('../partials/nav.ejs', userLogged) %>
	</header>
	
		<!-- CONTAINER LOGIN -->
		<div class="login-register">

			<!-- TITULO -->
			<div class="login-register-titulo">
				<h1>Ingresa tu cuenta</h1>	
			</div>
		


			

			<!-- FORMULARIO -->
			<form class="formulario" action="/users/login" method="POST">
				
			

			<!-- EMAIL -->
			<div class="campo-input">
				<input class="input-registro" type="email" id="email" name="email" placeholder="Email"
				<% if(typeof errors != "undefined"){%>
					value="<%= errors.reqEmail %>"
				<%}%>>
				<%if(typeof errors != "undefined"){%>
					<% for(let i = 0 ; i < errors.errors.length ; i++){ %>
						<%if(errors.errors[i].param == "email"){%>
							<p class="error-total">* <%= errors.errors[i].msg %></p>
							<% break; %>
						<%}%>
					<%}%>
				<%}%>
				<%if(typeof errors != "undefined"){%>
					<% for(let i = 0 ; i < errors.errors.length ; i++){ %>
						<% if(errors.errors[i].param == "email") {%>
							<% break; %>
						<%}%>
						<%if(errors.errors[i].param == "contrasenia" ){%>
							<p class="error-total" >* <%= errors.errors[i].msg %></p>
							<% break %>
						<%}%>
					<%}%>
				<%}%>
			</div>

			<!-- CONTRASEÑA -->
			<div class="campo-input">
				<input class="input-registro" type="password" id="contrasenia" name="contrasenia" placeholder="Contraseña">
				<% if(typeof credencialesInvalidas != "undefined"){ %>
					<p class="error-total">* <%= credencialesInvalidas %></p>
				<% } %>
				<% if(typeof contraseniaCambiadaEmail != "undefined"){%>
					<p class="error-total" style="color: green;"><%= contraseniaCambiadaEmail %></p>
				<%}%>
			</div>

			<div class="campo-input">
				<div>
					<label for="recordameLogin">Mantener mi sesión iniciada</label>
					<input type="checkbox" id="recordameLogin" name="recordameLogin">
				</div>
			</div>
				
			
			<!-- CONTRASEÑA OLVIDADA -->
			<p class="contraseña-olvidada"><a href="#" id="btn-contrasenia-olvidada">¿Olvidaste tu Contraseña?</a></p>
			
			<!-- BOTON INGRESAR -->
			<div class="campo-input">
				<button class="input-button" type="submit" id="ingresar" value="ingresar"> Ingresar</button>
			</div>
			</form>
			
			<div class="background-modal-contrasenia-olvidada" id="background-modal-contrasenia-olvidada">
				<div class="modal-content-contrasenia-olvidada">
					<span class="close-content-contrasenia-olvidada"><i class="far fa-times-circle "></i></span>
					<form action="/users/login/contraseniaOlvidada" method="POST">
						<p>¿Seguro que desea cambiar su contraseña? <br>
							Se le enviara al mail correspondiente una nueva contraseña, la cual usted luego podra cambiar desde su perfil.</p>
						<div class="form-inputs-contrasenia-olvidada">
							
								<input type="email" name="emailDeCambioContrasenia" placeholder="Escriba su email">
								
								<button type="submit">Enviar Nueva Contraseña</button>

						</div>						
					</form>
				</div>
			</div>
			


			<!-- REGISTRARSE -->
			<p class="registrate-usuario">¿Aún no tenés usuario? <a href="/users/register">¡Registrate!</a></p>
		</div>
				<!-- FOOTER -->
				<footer class="login-footer">
					<%- include('../partials/footer.ejs') %>
				</footer>

</body>
</html>