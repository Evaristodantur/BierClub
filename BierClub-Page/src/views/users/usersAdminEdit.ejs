<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BierClub - Todos los Productos</title>
	<link rel="icon" href="/images/faviconLogo.ico"/>
    <script src="/js/userAdmin.js" type="text/javascript"></script>
	<%- include('../partials/head.ejs') %>
</head>

<body>
	<!-- NAV -->
	<header>
		<%- include('../partials/nav.ejs', userLogged) %>
	</header>
    <div class="container-users">

        <!-- MODAL DE ELIMINAR-->
        <div id="<%= usuarios.id %>" class="modal-eliminar-usuario modal-eliminar-un-usuario">

            <!-- Modal content -->
            <div class="modal-content-eliminar-usuario">
              <p style="text-align: center;">¿Está seguro que desea eliminar este usuario?</p>
              <div class="eliminar-usuario-container" style="width: 100%; display: flex;">

                <button
                  class="input-button button-enteros input-button-cancel-delete"
                  style="width: 80%;"
                >
                  No
                </button>

                <form
                style="width: 100%; display: flex; justify-content: space-around; margin-left: 0;"
                  action="/users/eliminarUser/<%= usuarios.id %>?_method=DELETE"
                  method="POST"
                >
                
                  <button
                    type="submit"
                    style="width: 80%; margin-left: 0;"
                    class="input-button button-enteros button-eliminar"
                  >
                    Si
                  </button>
                </form>
              </div>
            </div>
          
          </div>



        <h1>Usuario: <%= usuarios.name %></h1>
        <section class="usuarios-listaCompleta usuario-lista-unica">
            <div class="usuarios-lista">
                <form action="/users/usersAdmin/<%= usuarios.id %>" method="POST">
                <div class="container-input">
                    <h3>ID: <%= usuarios.id %></h3> 
                    <input class="inputModificar" type="hidden" id="id" name="id" value="<%= usuarios.id %>">
                </div>
                <div class="container-input">
                    <h3>Nombre:</h3> 
                    <input class="inputModificar" type="text" id="nombre" name="nombre" value="<%= usuarios.name %>">
                </div>
                <div class="container-input">
                    <h3>Email:</h3> 
                    <input class="inputModificar" type="email" id="email" name="email" value="<%= usuarios.email %>">
                </div>
                <div class="container-input">
                    <input class="inputModificar" type="hidden" id="contrasenia" name="contrasenia" value="<%= usuarios.password %>">
                </div>
                <div class="container-input">
                    <h3>Admin:</h3> 
                    <input class="inputModificar" type="checkbox" id="admin" name="admin" <%if (usuarios.admin == 1){%>
                        checked
                    <%}%>>
                    
                </div>
                <button class="button-modificar button-enteros" type="submit"> Modificar datos</button>

                
                </form>

                
                <button onclick="eliminarUsuario(<%= usuarios.id %>)" class="input-button button-enteros button-eliminar">Eliminar usuario</button>
                

            </section>
        </div>
            	

</body>
</html>