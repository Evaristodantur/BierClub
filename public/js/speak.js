/*let titulo = document.title;
let texto = `Hola, soy tu amigo el hombre araña y entraste a ${titulo}`;
        const hablar = (texto) => speechSynthesis.
        speak(new SpeechSynthesisUtterance(texto));

        hablar(texto);
*/